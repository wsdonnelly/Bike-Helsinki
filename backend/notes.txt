
in backend/:
npm init -y
npm install express body-parser
npm install node-addon-api          # for N-API C++ wrappers
npm install node-gyp --save-dev    # for building addons

build addon
cd backend/bindings
rm -rf build && npx node-gyp configure && npx node-gyp build
USE REBUILD --> same as above
npx node-gyp rebuild

node-gyp rebuild --release

TO RUN:
node index.js